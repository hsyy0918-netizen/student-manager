<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jxd.stuManger.dao.ICourseDao">
    <sql id="BaseCol">
        course_id, course_name, teacher_id, course_time, classroom, class_week, course_type, college_id, score
    </sql>

<!-- -->
    <insert id="insertCourse" parameterType="course">
        insert into course (course_id, course_name, teacher_id, course_time, classroom, class_week, course_type, college_id, score)
        values (#{courseId},#{courseName},#{teacherId},#{courseTime},#{classroom},#{classWeek},#{courseType},#{collegeId},#{score})
    </insert>

    <update id="updateCourse" parameterType="course">
        update course set course_id = #{courseId},course_name = #{courseName},
                          teacher_id = #{teacherId},course_time = #{courseTime},
                          classroom = #{classroom},class_week = #{classWeek},
                          course_type = #{courseType},college_id = #{collegeId},score =#{score}
    </update>

    <delete id="delCourse" >
        delete from course where course_id = #{courseId}
    </delete>

    <select id="selectAllCourse" resultType="course">
        select <include refid="BaseCol"></include>
        from course
    </select>

    <select id="selectCourseById" resultType="com.jxd.stuManger.model.Course">
        select <include refid="BaseCol"></include>
        from course where course_id = #{courseId}
    </select>

    <select id="selectCoursesByName" resultType="com.jxd.stuManger.model.Course">
        select <include refid="BaseCol"></include>
        from course where course_name like concat('%', #{courseName}, '%')
    </select>

    <select id="selectTeacherCourses" resultType="com.jxd.stuManger.model.Course">
        select <include refid="BaseCol"></include>
        from course where teacher_id = #{teacherId}
    </select>

    <select id="isCourseSelected" resultType="java.lang.Boolean">
        select exists(select 1 from course where course_id = #{courseId})
    </select>

</mapper>